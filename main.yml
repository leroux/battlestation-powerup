---
- hosts: localhost
  connection: local
  gather_facts: yes

  vars_files:
    - vars/main.yml

  roles:
    - homebrew

  tasks:
    - include: tasks/dotfiles.yml
    - include: tasks/preferences.yml
